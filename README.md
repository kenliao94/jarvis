A thin CLI client to OpenAI

To install, navigate to the project repo

sudo chmod +x jarvis
export PATH=$PATH:$(pwd)
echo "OAI_KEY=<Your OAI Key>" >> .env
echo "ORG_ID=<Your org id key>" >> .env

road map
Integrate with pip 
Integrate Langchain
